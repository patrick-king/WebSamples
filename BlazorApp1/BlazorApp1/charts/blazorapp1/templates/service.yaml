apiVersion: v1
kind: Service
metadata:
  name: {{ template "blazorapp1.fullname" . }}
  labels:
    app: {{ template "blazorapp1.name" . }}
    chart: {{ template "blazorapp1.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blazorapp1.name" . }}
    release: {{ .Release.Name }}
